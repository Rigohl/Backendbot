[phases.setup]
nixPkgs = ['python311', 'gcc', 'gfortran']

[phases.install]
cmds = [
    'pip install -r requirements.txt'
]

[phases.build]
cmds = [
    'echo "Build completed for BackendBot"'
]

[start]
cmd = 'python railway_start.py'