# Índice de Librerías

- **FastAPI**: `fastapi.md` — comandos de despliegue, configuración de `pydantic-settings`, Docker y buenas prácticas.
- **Uvicorn**: `uvicorn.md` — CLI, opciones relevantes (`--workers`, `--proxy-headers`, `--root-path`), uso programático.
- **SQLAlchemy**: `sqlalchemy.md` — configuración de motores, sesiones, y recomendaciones para pool/timeout.
- **Pydantic**: `pydantic.md` — `BaseSettings` / `pydantic-settings` ejemplos y recomendaciones.
- **HTTPX**: `httpx.md` — cliente HTTP async, patrones de retries y uso en tests.

- **psycopg2**: `psycopg2.md` — ejemplos de conexión, pooling y buenas prácticas con PostgreSQL.
- **python-dotenv**: `python-dotenv.md` — uso de `.env`, CLI, y notas de seguridad.

Archivos generados automáticamente por el asistente para centralizar guías del MCP.
